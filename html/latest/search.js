const SEARCH_INDEX = [
  {
    "section": "About",
    "folder": "About",
    "title": "What is Mianotes?",
    "path": "about/what-is-mianotes-eae08671.html",
    "text": ":::note Note This documentation is written and maintained by AI agents using Mianotes. If you find anything wrong, unclear, or misleading, please open an issue on GitHub so we can review and fix it. Thank you. ::: Mianotes is a local first knowledge repository for people and AI agents. It converts notes, links, documents, meeting recordings, videos, articles, images, and AI output into clean Markdown you can search, organise, edit, and reuse. People use the web app to read, edit, tag, and share notes. AI agents use the API or MCP server to create notes, retrieve context, search existing knowledge, update documentation, and leave handoff messages. Mianotes is built with open source tools, including Microsoft MarkItDown and FFmpeg for file conversion and Ollama for local AI models. Why it exists Mianotes exists because project knowledge is no longer created by people alone. Teams collect research, links, documents, meeting recordings, screenshots, notes, and AI outputs as they work. At the same time, AI agents are writing code, making decisions, fixing bugs, generating summaries, proposing changes, and producing context that often disappears when the session ends. The problem is not just where information is stored. The problem is that people and agents do not have a shared, consistent way to capture it, structure it, and reuse it later. Mianotes gives both people and agents a local place to write things down. A developer can save source material, meeting notes, research, and project context. An agent can document what it changed, explain why it made a decision, ask questions, leave handoff notes, or save useful findings before its session ends. Another agent can then search that same knowledge and continue from the right context. Mianotes also helps you collect information from many sources quickly, including files, links, audio, images, videos, and AI responses, then convert that material into clean Markdown that people can read and agents can use. The goal is to standardise how project knowledge moves between people, tools, and agents, without losing control of where that knowledge lives. What Mianotes stores Mianotes keeps user facing note content as Markdown files under the active data folder: The workspace SQLite database lives in the Mianotes data directory: SQLite stores indexes and metadata such as folders, tags, notes, share records, and jobs. Global users and sessions live in data/system.db . The note text stays in Markdown files inside the workspace folder. Meet Mia Mia is the built in Mianotes agent. Mia helps convert messy input into durable Markdown notes, improve structure, extract useful information, summarise long content, and prepare notes for reuse by humans or other agents."
  },
  {
    "section": "About",
    "folder": "About",
    "title": "Use cases",
    "path": "about/use-cases-73626998.html",
    "text": "Mianotes is useful whenever humans and agents need a shared, searchable, local knowledge base. Developers using AI agents Use Mianotes as the documentation layer for Codex, Claude Code, Cursor, OpenClaw, VS Code agents, Slack bots, and local automation. Every local agent has a goal, and every agent needs a place to report back. Mianotes gives agents a local workspace where they can explain what they did, how they did it, what changed, and what the outcome was. Agents can save: Task summaries Implementation notes Decisions and trade offs Changed files and why they changed Debugging findings Test results Questions and proposals Project context for the next agent run This gives the person orchestrating the work a clear record of what happened: what their agents are doing, how they are doing it, and the result of each task. All of that context is captured and centralised in one place. AI agents and tools Agents can use Mianotes as long term memory that stays under your control. An agent can: Create a folder for a task Add notes as it works Convert source files to Markdown Tag notes for discovery Search earlier notes before making changes Update a note when new evidence arrives Leave handoff notes for review Students Students can use Mianotes to turn study material into clean, searchable notes. Upload lecture slides, book pages, whiteboards, images, handwritten notes, or audio recordings, then ask Mia to structure the material as Markdown. Biology revision History essays Maths examples Lecture notes Useful Mia prompts include: Small teams Small teams can use Mianotes as a shared local knowledge base. Typical folders: Spec and PRD files Research Meeting notes Internal decisions Roadmaps Launch plans Team members can add tags across folders, improve notes, and create read only share links when someone outside the workspace needs access to a single note. Researchers Researchers can use Mianotes to keep links, papers, documents, notes, prompts, and summaries in one searchable place. Mianotes is especially useful when source material is messy because URL and file ingestion can preserve the original source next to the generated Markdown note. Families and everyday use Mianotes also works as a private home knowledge base. Examples: Homework School documents Trip plans Medical appointment notes Images of forms or letters The value is not only AI. The value is that the final note is readable text, stored locally, and easy to search."
  },
  {
    "section": "About",
    "folder": "About",
    "title": "Core concepts",
    "path": "about/core-concepts-ca799f8f.html",
    "text": "This page explains the words used throughout the documentation. Workspace A Mianotes workspace is a top level knowledge area backed by a local folder. The workspace folder contains Markdown notes, source files, and published output. The private SQLite database for that workspace lives in the Mianotes data directory at data/workspaces/<workspace id .db . Small groups can share a Mianotes app by using the same master password. Everyone can read shared notes, browse by folder, browse by user, and add notes to active folders. User A user is a human account in the current Mianotes folder. The first user becomes the admin for that folder. Browser users authenticate with a session cookie. Agents should use API tokens instead of browser sessions. Folder A folder is a shared workspace for related notes. Folder rows are stored in SQLite, while each folder also owns a filesystem directory under the workspace markdown/ directory. A folder can be archived. When archived, the folder is hidden from normal lists and moved under the workspace archive area so active folders stay clean. Note A note is the main unit of knowledge. The note body is a Markdown file on disk. Metadata lives in SQLite. A note can have source files, tags, share links, and job history. A note filename normally looks like: Example: Source file A source file is the original file or downloaded HTML used to create a note. For file uploads, Mianotes stores the uploaded file under: Source files are kept next to the generated Markdown note but ignored by the folder level .gitignore so Git backups can store notes without committing original uploads. Tag Tags provide cross folder grouping. Notes can be tagged with short labels such as research , planning , client , or release . Tags are useful for both humans and agents because they make related notes easier to discover. Job A job represents background work, such as parsing an uploaded file or indexing a URL. Jobs move through clear status transitions: Clients should poll the job endpoint when a file or URL note is still being parsed. Mia Mia is the built in AI assistant. Mia works through backend service boundaries, not frontend only behaviour. The backend owns permissions, persistence, parsing, and the LLM provider calls."
  },
  {
    "section": "Getting Started",
    "folder": "Getting Started",
    "title": "Installation",
    "path": "getting-started/installation-cee981f1.html",
    "text": "There are two supported ways to install Mianotes. Install from package Use this if you want Mianotes installed into the normal system locations for your operating system. macOS: mianotes.pkg Ubuntu: mianotes.deb Install with Docker Use this if you want Mianotes to run in a container on a laptop, workstation, or server. Docker keeps the web service and dashboard together and makes it easier to run, stop, update, or host multiple Mianotes environments. Install from GitHub Use this if you are a developer, contributor, or agent tool builder who wants editable source checkouts of the web service and dashboard."
  },
  {
    "section": "Getting Started",
    "folder": "Getting Started",
    "title": "Installing from package",
    "path": "getting-started/installing-from-package-32e583cc.html",
    "text": "Install from package when you want Mianotes installed into normal system locations and started by the operating system. This is the recommended path for people who want to use Mianotes rather than work on the code. macOS Download the latest package: Open mianotes.pkg and follow the installer. The package installs Mianotes into: After installation, Mianotes runs as two launchd services: com.mianotes.web service on port 8200 com.mianotes.dashboard on port 8201 Open Mianotes with: Run a local health check with: The macOS package bundles the runtime Mianotes needs to run, including Python, ripgrep, ffmpeg, and ffprobe. Apple Silicon packages also include a bundled Tesseract OCR binary and English OCR data. mianotes doctor shows which bundled tools are available on the current Mac and whether any optional system fallback tools are being used. Ubuntu Download the latest Debian package: Install it with: The package installs Mianotes into: After installation, Mianotes runs as two systemd services: mianotes web service on port 8200 mianotes dashboard on port 8201 The Ubuntu package installs the required system packages for parsing and searching notes, including ripgrep , ffmpeg , and tesseract ocr . Open Mianotes with: Run a local health check with: Useful commands mianotes uninstall removes the app services, installed app files, settings, users, API keys, workspace configuration, and workspace databases. It keeps Markdown notes, source files, and published HTML under: macOS: /Library/Application Support/Mianotes/data/ Ubuntu: /var/lib/mianotes/data/ After reinstalling Mianotes, add a folder as a workspace to import compatible Markdown notes again. First run Open the web app, create the first user, then create or switch workspace."
  },
  {
    "section": "Getting Started",
    "folder": "Getting Started",
    "title": "Installing with Docker",
    "path": "getting-started/installing-with-docker-f1364575.html",
    "text": "The Docker install uses one Mianotes container. The container runs the web service and the dashboard together. Requirements Install Docker Desktop, Docker Engine, or another Docker compatible runtime that supports Docker Compose. You do not need to install Python, Node.js, FFmpeg, FLAC, or Tesseract on the host machine. Those tools are provided inside the Mianotes container. Folder layout Create a folder for Mianotes: Download the official Compose file and start Mianotes: Open: If Mianotes is running on another machine, replace localhost with the server address. Mianotes stores persistent data in a data/ folder next to docker compose.yml : The data/ folder is mounted into the container at /data . This means files written by Mianotes inside the container are saved on your computer or server outside the container. docker compose.yml The downloaded docker compose.yml should look like this. You can edit it before starting Mianotes if you need to change ports, volumes, or environment variables. If you edited the file manually, start Mianotes with: View logs Stop Mianotes This stops and removes the container, but it does not delete your data/ folder. Update Mianotes Back up Mianotes Back up the data/ folder: The backup includes your system database, workspace databases, Markdown files, source records, and published HTML. Remove Mianotes Stop the container: Then remove the Mianotes folder if you no longer need the data: Only delete the folder if you are sure you no longer need your notes."
  },
  {
    "section": "Getting Started",
    "folder": "Getting Started",
    "title": "Installing from GitHub",
    "path": "getting-started/installing-from-github-d22c4f44.html",
    "text": "Install from GitHub when you want editable source checkouts of Mianotes. This path is best for developers, contributors, and people wiring Mianotes into local agent workflows. What gets installed The GitHub installer creates two folders in the current directory: It then runs each app's own development installer. Requirements Install the system tools first. Mianotes currently supports Python 3.11, 3.12, 3.13, and 3.14 for source installs. You do not need to install MarkItDown manually. The web service installer creates a local Python virtual environment and installs Mianotes' Python dependencies there, including MarkItDown and the OCR plugin. macOS Install Node.js 20 or newer from nodejs.org or through your preferred package manager. Ubuntu and Debian Install Node.js 20 or newer before running the dashboard. Tool purpose: | Tool | Why Mianotes uses it | | | | | Python | Runs the FastAPI web service. | | Node.js and npm | Build and run the React dashboard. | | ripgrep | Searches saved Markdown notes. | | Tesseract | Performs local OCR for image uploads. | | ffmpeg | Enables audio and video parsing when needed. | | flac | Supports audio transcription conversion. | ffmpeg and flac are optional unless you want to parse audio or video sources. Tesseract is optional unless you want local image OCR. Install Run this from the folder where you want the source checkouts: The installer downloads the web service and dashboard repositories, installs their dependencies, and leaves you with editable local projects. Use a specific branch or tag Create .env file The web service reads configuration from mianotes web service/.env . For a local Ollama style model: For OpenAI: Do not add database or workspace variables unless you want to change the default file locations. By default, Mianotes stores system state under data/system.db , workspace databases under data/workspaces/ , and workspace content in the selected workspace folders. Start Mianotes The default local API URL is: In another terminal, start the dashboard: The dashboard URL is: Check that it is running Expected response: Apple Silicon Tesseract check On Apple Silicon Macs, make sure Tesseract is installed through the ARM Homebrew path. If the command prints Bad CPU type in executable , an old Intel binary is being used and image OCR will fail. Reinstall Tesseract, then restart the Mianotes service: Next steps 1. Open the web app. 2. Create the first user. 3. Create or switch workspace. 4. Add a note from text, file, or URL. 5. Connect an agent with an API token when you are ready."
  },
  {
    "section": "Getting Started",
    "folder": "Getting Started",
    "title": "First run",
    "path": "getting-started/first-run-b18fb985.html",
    "text": "The first run creates the first user, the master password, the default storage folder, and the first notes folder. What happens on first setup When no Mianotes workspace has been configured yet, the first user joins through: Mianotes then: 1. creates the first admin user; 2. stores the master password hash; 3. initialises data/system.db and data/workspaces/default.db ; 4. seeds the default Mianotes folder; 5. starts a browser session for the new user. The first user is the admin for that folder. Recommended first run flow The frontend can call: When the response contains: show a setup screen that explains that the first password becomes the master password for this Mianotes workspace. First folder After setup, create a folder for the work you want Mianotes to remember. Good starter folders: Mianotes Research Project notes Agent worklog School Receipts Family notes First note Start with a simple text note before uploading files. Example note: Then search for it: First file upload After text notes work, try a small file such as a .txt , .md , .pdf , .jpg , .png , or .mp4 . File and URL ingestion return a pending note plus a job. The conversion runs in the background. Clients should poll the job URL until the job reaches succeeded , then fetch the note. First agent connection Create or configure an API key, then test the session endpoint:"
  },
  {
    "section": "Getting Started",
    "folder": "Getting Started",
    "title": "Configuration options",
    "path": "getting-started/configuration-options-2a0c5347.html",
    "text": "Mianotes configuration uses settings.json for project defaults and environment variables with the MIANOTES prefix for secrets and local overrides. For normal local use, configure only the LLM provider and API key. settings.json The web service reads settings.json from the service root when it starts. Environment variables override values from this file. Example: Values beginning with env. are read from the process environment or .env . Do not put raw secrets in settings.json . The database adapter is currently sqlite . If MIANOTES DATABASE URL is unset, Mianotes uses the default local data/system.db . Common variables | Variable | Default | Description | | | | | | MIANOTES HOST | 0.0.0.0 or script default | Host interface for the web service. Use 127.0.0.1 for local only use. | | MIANOTES PORT | 8200 | Web service port. | | MIANOTES LLM PROVIDER | openai | LLM provider: openai , local , or openai compatible . | | MIANOTES LLM MODEL | gpt 5 nano | Text model used by Mia. | | MIANOTES LLM BASE URL | empty | Base URL for local or OpenAI compatible providers. | | MIANOTES LLM API KEY | empty | LLM provider API key or local placeholder. | | MIANOTES VLM API KEY | empty | Optional image capable model provider key. | | MIANOTES LLM IMAGE MODEL | empty | Optional multimodal OpenAI model for image OCR fallback. | | MIANOTES API KEY | empty | User scoped bearer token installed by Settings Connect tools. | | MIANOTES MAX PUBLISHED SITE DOWNLOAD BYTES | 262144000 | Maximum bytes allowed for a published site ZIP download. | | MIANOTES MAX PUBLISHED SITE DOWNLOAD FILES | 5000 | Maximum file count allowed in a published site ZIP download. | Ports Mianotes services use the 8200 range: To start on another port during development: Storage defaults Generated notes and source files live inside each workspace folder: Folder directories are filesystem safe slugs and are unique inside a workspace. Source files live under each folder's sources/ directory. LLM provider values OpenAI Local Ollama style provider Other OpenAI compatible endpoint Tool access variables Settings Connect tools installs the API URL, user scoped key, and user email for Codex, Claude Code, and compatible tools: Agent clients send it as: Scoped API tokens are also available through /api/tokens when an automation needs narrower permissions."
  },
  {
    "section": "Getting Started",
    "folder": "Getting Started",
    "title": "Ollama, OpenAI, and local models",
    "path": "getting-started/ollama-openai-and-local-models-e18d740e.html",
    "text": "Mia calls language models through a provider boundary. This lets one Mianotes app use OpenAI, a local Ollama style server, or another OpenAI compatible endpoint without changing the REST API or MCP server. Local Ollama setup Install Ollama, start it, then run a model. macOS with Homebrew In another terminal: Check that the model is installed: Use this .env configuration: 127.0.0.1 means the machine running the Python web service. If Mianotes and Ollama are both running on your Mac, this is correct. If the backend runs on another box, such as a home server or Senseibox, then 127.0.0.1 points to that box instead of your Mac. In that case, use the Mac's LAN IP address: Ollama must also be configured to listen on the network before another machine can reach it. OpenAI setup With OpenAI configured, gpt 5 nano can also be used as an image fallback when local OCR is not enough. To use a different OpenAI model for image OCR: OpenAI compatible setup Use this when you have another hosted or local endpoint that follows an OpenAI compatible chat completions interface. Image uploads Image files are handled in stages: 1. Mianotes runs MarkItDown's image converter. 2. Mianotes tries local Tesseract OCR for .jpg , .jpeg , .png , .tif , and .tiff . 3. For screenshots and UI captures, Mianotes also tries a preprocessed OCR pass with better contrast and size. 4. If local OCR cannot extract useful text and OpenAI is configured, Mianotes sends the image to a multimodal model as a cloud fallback. 5. If no fallback is configured, the note is saved with a short Mia message explaining that no text could be extracted. 6. Local text models such as llama3.2:3b cannot process images directly. Tesseract handles text heavy images locally. Audio and video Install ffmpeg if you plan to parse audio or video sources. HTML, document, and text conversion can ignore the ffmpeg warning."
  },
  {
    "section": "Getting Started",
    "folder": "Getting Started",
    "title": "Troubleshooting",
    "path": "getting-started/troubleshooting-098fbe52.html",
    "text": "Use this page when setup or ingestion does not behave as expected. Service is not reachable Check health: If the service is running on another port, set MIANOTES API URL : 401 Not authenticated The request is missing a valid browser session or bearer token. For API clients: Make sure the token is in the environment used by the shell, agent, or MCP process. 403 Permission denied The token was accepted, but it does not have the scope required for that endpoint. Use Settings Connect tools to install a user scoped key for trusted local agents, or create a scoped token with the right scopes: Search returns 503 Mianotes searches Markdown files with ripgrep . Install it and restart the service. macOS: Linux: Image OCR fails on Apple Silicon Run: If you see Bad CPU type in executable , reinstall Tesseract with ARM Homebrew: Restart Mianotes after reinstalling. File or URL note stays pending File and URL ingestion create a background job. Poll the job URL returned in the creation response: When the job reaches succeeded , fetch the note from note api url . Mia returns 503 Mia is not configured or the configured LLM provider is unavailable. Check: MIANOTES LLM PROVIDER MIANOTES LLM MODEL MIANOTES LLM BASE URL MIANOTES LLM API KEY whether Ollama or the chosen local endpoint is running MCP cannot authenticate The MCP process needs access to the same API URL and token as other agent clients: If your MCP client starts the server from a different shell, make sure that shell can see the same environment variables."
  },
  {
    "section": "Workflow",
    "folder": "Workflow",
    "title": "Ownership",
    "path": "workflow/ownership-6271c5da.html",
    "text": "Mianotes is built around shared knowledge with light ownership. People and AI agents can both create folders, add notes, improve notes, and leave handoff notes. A normal workflow 1. Create or join a Mianotes workspace. 2. Create a folder for a project, topic, class, client, trip, or research area. 3. Add a note from text, file, image, URL, or audio. 4. Let Mianotes convert the source into Markdown. 5. Review the note in the web app. 6. Add tags for discovery. 7. Ask Mia to summarise, extract, or restructure content when useful. 8. Share a read only link when someone outside the workspace needs access. Shared workspaces Teams, families, developers, researchers, and other small groups can use the same master password to join a Mianotes workspace. Everyone can: read shared notes browse by folder browse by user add notes to active folders search saved Markdown Ownership still matters: the note creator can edit or delete their note; the folder creator can archive their folder; admins can manage any note or folder. Working with agents A useful pattern is: 1. A person creates the project folder. 2. An agent creates task notes as it works. 3. The agent tags the notes by topic or status. 4. The person reviews the notes, edits what matters, and shares the final result if needed. Good folder patterns Use folders for high level areas. Examples: | Folder | Good tags | | | | | Mianotes | api , ui , security , release | | Client Alpha | meeting , decision , todo , invoice | | School | biology , revision , homework | | Research | paper , summary , quote , follow up | | Agent worklog | codex , claude , bug , done |"
  },
  {
    "section": "Workflow",
    "folder": "Workflow",
    "title": "Folders, notes, and tags",
    "path": "workflow/folders-notes-and-tags-2a58bd40.html",
    "text": "Folders, notes, and tags are the main organisation tools in Mianotes. Folders Folders are group visible. Any signed in user can create a folder and add notes to any active folder. Only the folder creator or an admin can archive a folder. When a folder is archived, the folder directory is moved out of the active data area and into data/.archived/ . The Markdown notes and source files are preserved. Notes Notes store the knowledge people and agents reuse. A note can be created from: plain text Markdown file upload URL image audio or video when dependencies are installed The final note body is stored as a Markdown file on disk. Source files When a note is created from a file or URL, Mianotes keeps the source next to the generated note. Typical layout: This makes the note readable while keeping source material available for review. Tags Tags are global labels attached to notes. Use tags when a note belongs to more than one category. For example, a note can live in the Client Alpha folder while also being tagged invoice , urgent , and legal . Tags are also useful when publishing. You can classify notes before export, then publish only the notes that match the tags you choose. Stars Stars are private per user. Starring a note does not make it starred for everyone else. Use stars for notes you personally need to revisit. Publishing and sharing A note can be shared with a read only link. Share links are note level and revocable. A share link does not grant full workspace access."
  },
  {
    "section": "Workflow",
    "folder": "Workflow",
    "title": "Sharing",
    "path": "workflow/sharing-133509d5.html",
    "text": "Mianotes supports read only note sharing outside the workspace. Share links Use a share link when someone outside the Mianotes workspace needs access to one note but should not join the whole workspace. Important properties: Share links are note level and read only. A share link does not grant full workspace access Share links can be revoked Admins can share any note Normal users can share notes they created Each share link includes the workspace id so Mianotes opens the note from the right workspace Source files on shared notes When sharing is enabled, a guest readable shared source file endpoint can return source files attached to the shared note.& x20; Only share notes whose source files are safe for the recipient to see. Practical sharing checklist Before sharing a note: 1. Read the full Markdown note. 2. Check attached source files. 3. Remove private details or copy only the final content into a clean note. 4. Generate the share link. 5. Revoke the link when it is no longer needed."
  },
  {
    "section": "Workflow",
    "folder": "Workflow",
    "title": "Publishing static sites",
    "path": "workflow/publishing-static-sites-55b4c13d.html",
    "text": "Mianotes can publish selected notes as a static documentation site. This is useful when a folder has become reference material, project documentation, or a small knowledge base that should be browsed outside the main app. Publishing creates HTML files under the active data folder. It does not replace the original Markdown notes or their source files. Publish from the web app 1. Open the Mianotes web app. 2. Click Publish in the sidebar. 3. Choose all folders, one folder, or another available scope. 4. Review the generated navigation. 5. Review the site configuration. 6. Click Publish . After publishing, Mianotes shows actions to preview the static site in your browser or download the generated site as a ZIP file. Navigation and new notes The Navigation block always contains every note that will appear in the static site for the selected scope. It is regenerated from the current publishable notes each time you open the publish draft. The New notes block is only a change summary. It lists notes whose generated paths were not present in the previous publish navigation. If you publish again without adding notes, New notes should be empty. Site configuration | Key | Description | | | | | brand | Name shown in the static site header. | | version | Version folder used for the generated site, such as 1.0.0 . | | headerLinks | External links shown after version links. | | showPreviousVersions | When true , show up to the latest 3 published versions in the header. | | footerHtml | Footer HTML shown at the bottom of article pages. | Themes Mianotes ships two static publishing themes: | Theme ID | Name | Use | | | | | | mialight | Mialight | Default light documentation theme. | | miadark | Miadark | Dark documentation theme. | Both themes render Markdown tables, fenced code blocks, admonitions, article navigation, search, and an On this page column generated from page headings. Markdown rendering The static renderer converts common Markdown into HTML before writing the site. This includes headings, paragraphs, bullet lists, fenced code blocks, Markdown tables, inline code, strong text, links, and GitHub style admonitions such as [!TIP] and [!WARNING] . If the first Markdown heading repeats the note title, Mianotes removes the duplicate heading so article pages do not show the title twice. Generated files A published version is written under: The public root redirect and version navigation live under: Generated HTML includes: Downloading the site The publish response includes a download url . The ZIP archive contains the version directory plus root files needed for version navigation. Serving published files Static HTML is public under /html/... . Published Markdown is available under /markdown/... . Authenticated users and valid bearer token clients can read stored Markdown files. Public callers can only read Markdown or source files that belong to notes marked as published. Database files are never served."
  },
  {
    "section": "Connecting tools",
    "folder": "Connecting tools",
    "title": "Agent overview",
    "path": "connecting-tools/agent-overview-45896e8a.html",
    "text": "Mianotes is designed for collaboration between humans and AI agents. AI agents write plans, explain changes, debug issues, summarise research, and leave useful context behind. But most of that work disappears inside temporary chats, IDE sidebars, Slack threads, and terminal sessions. Mianotes gives that work a place to live. Agents can save decisions, implementation notes, summaries, source links, files, images, and project context into clean Markdown notes, so the next agent can pick up the same context without asking you to explain everything again. Agents should use Mianotes as a local documentation layer. They can save useful context while they work, then retrieve that context later through search, API calls, or supported tool integrations. What agents can do An agent can: create folders for tasks, experiments, projects, or research threads add notes from text create notes from URLs upload files through the REST API convert documents, links, images, and audio into Markdown notes read notes update notes set tags search Markdown notes leave handoff messages send Mia prompts through the prompt endpoint when an AI provider is configured Recommended agent behaviour Agents should document durable information, not every temporary thought. Good agent notes include: decisions made relevant commands run files changed why an implementation changed bugs found and fixed test results next steps links to source material warnings about uncertainty Avoid filling Mianotes with low value logs that no human or agent will reuse. Access model Agents should use the REST API, not browser cookies. The easiest setup is to open Settings Connect tools , create an install script, and run that command on the machine where Codex, Claude Code, or another compatible tool runs. The install script writes: The values are saved in /.mianotes/env , and the latest Mianotes SKILL.md is installed for Codex and Claude Code. The script never prints the API key. MIANOTES API KEY belongs to the signed in user who generated the install script. Every API request made with that key uses that user's permissions and workspace access. If the user revokes the key in Settings, tools using that key stop working until the user creates a new install script. Agents can call the REST API directly with: Tools that need a short lived session can exchange the API key at /api/auth/agent session , but direct bearer token API calls are supported. Browser sessions are for humans using the web app. Do not use browser cookies for agents. Workspace targeting Mianotes can manage multiple workspaces. Agents should target the requested workspace explicitly instead of assuming the active browser workspace. REST clients should send the workspace id with each workspace content request: MCP clients use the same backend permission model. When an MCP tool accepts a workspace argument, pass the requested workspace there instead. Use conversational prompts when asking an agent to use Mianotes: Search the My Project workspace for deployment notes. Get context from the Docs folder in the My Project workspace. Search the Docs folder in the My Project workspace for publishing workflow. Read the Architecture note in the Docs folder of the My Project workspace. Save this in the Architecture folder of the My Project workspace. Examples: When saving into a Mianotes workspace and folder, create a useful title from the content if the user did not provide one. Converting source material Agents can ask Mianotes to convert source material into Markdown notes. Examples: Links and files are ingested as background jobs. Agents should create the note, poll the returned job URL until it succeeds, then fetch the final Markdown note."
  },
  {
    "section": "Connecting tools",
    "folder": "Connecting tools",
    "title": "API token",
    "path": "connecting-tools/api-token-8cf72171.html",
    "text": "Mianotes API clients authenticate with bearer tokens. This page explains how to get MIANOTES API URL and MIANOTES API KEY , then exchange them for a short lived session token that an agent, script, or other tool can use with the REST API. The easiest way to get started is through the web app. Sign in, open Settings , and generate an install URL. Mianotes creates a one time install link that expires after 24 hours. Run the command on the same machine as Codex, Claude Code, or another local tool. The installer writes the connection details to /.mianotes/env and installs the Mianotes skill for Codex and Claude: The downloaded shell script does not display the API key. It exchanges the one time code while it runs, writes the key to /.mianotes/env , and marks the code as used. Rerun the Settings action if you need a new command. Get a session token from the API URL and key The install script gives agents two values they need to talk to Mianotes: MIANOTES API URL MIANOTES API KEY Use those values to request a short lived session token: The response contains a short lived bearer token. Use that session token for follow up API requests. Mianotes identifies the user from MIANOTES API KEY , so requests made with the session token use that user's permissions and workspace access. The raw API key is never embedded in the session token. How Mianotes stores the key The raw key is a secret. Treat it like a password. Mianotes compares bearer tokens by hashing the presented token and comparing it with the public verifier stored in data/system.db . The database does not store the raw key. There are two normal places the raw key lives: the one time installer writes it to /.mianotes/env on the agent machine tools can receive the same variables through their own environment On install script redemption, Mianotes stores only the derived public verifier in data/system.db . The generated token belongs to the signed in user and keeps working until that user regenerates it or revokes it. Get the API URL and key in the web app This is the recommended path for most people because it avoids hand writing API requests. 1. Start the Mianotes web service. 2. Open the Mianotes web app. 3. Sign in as an admin user. 4. Open Settings . 5. Click Generate URL . 6. Copy and run the command on the machine where your local agent runs. 7. Open a new terminal session so the exported environment is available. For a local shell: For a project .env file: Then create a short lived session token from the installed API URL and key: Create an install URL from the API You can also create the one time install URL from the REST API. Use this when you are setting up Mianotes from a script or when the web app is not available. You must call this endpoint as an admin. The simplest way is to sign in through the browser first, then send the request with the browser session cookie. If you are scripting the whole flow, first resolve the admin user's ID: Then sign in and save the session cookie: Then create a one time skill installer with that saved cookie. The endpoint returns a command, not the raw API key: The response contains a short lived install command: Run the command on the agent machine. When the script runs, Mianotes creates the per user API token and writes it to /.mianotes/env . After the installer has run, exchange the API key for a short lived agent session: If you already have a bearer token for your user, you can create a one time skill installer without a browser cookie: Use the token from JavaScript Use the token from Python User tokens and scoped tokens MIANOTES API KEY is the key installed for the current user. It is best for trusted local agents and scripts that should act as that user. Mianotes also supports scoped per user API tokens through /api/tokens . Use scoped tokens when a tool should have narrower permissions, such as read only note access. Create a scoped token: Example response: The raw scoped token is also returned only once. Common checks Check that your shell has the key: Check that the service is reachable: Check authentication: If you get 401 , the bearer token is missing or invalid. If you get 403 , the token was accepted but does not have the required scope for that endpoint. Keep tokens private Do not commit API tokens to git. Do not paste real tokens into examples, screenshots, issues, or logs. If a token may have been exposed, create a new key and update the environments used by your agents and tools."
  },
  {
    "section": "Connecting tools",
    "folder": "Connecting tools",
    "title": "MCP server",
    "path": "connecting-tools/mcp-server-1de5df7f.html",
    "text": "Mianotes ships a stdio MCP server for compatible AI agents. The MCP server calls the same REST API as other agent clients, so normal token scopes and backend permission checks still apply. Install Script The easiest setup is to create an Install Script in Mianotes Settings Connect tools and run it on the same machine as your MCP client. That command writes these API values to /.mianotes/env : MCP clients can then start mianotes mcp without passing API credentials manually. On startup, the MCP server exchanges MIANOTES API KEY for a short lived agent session. The MCP server then uses the returned session token for tool calls. Mianotes identifies the user from the API key, so MCP requests use the same user permissions and workspace access as direct API requests. Configure Codex Codex needs a stdio MCP server entry that starts mianotes mcp . Step 1 . Make sure Mianotes is running. Step 2 . Create an install script in Mianotes Settings and run it on the same machine where Codex runs. Step 3 . Check that the MCP command is available on the same machine where Codex runs: If it is not in PATH , use the full path to the command, for example: Step 4 . Install Codex CLI Step 5 . Add the MCP server: If you installed Mianotes from package, use command mianotes mcp . If you installed it from source, use command /path/to/mianotes web service/.venv/bin/mianotes mcp . Step 6 . Verify the server is configured: Step 7 . Within Codex (desktop app), ask Mia to do something: Configure Claude Code Claude Code also needs a stdio MCP server entry. Step 1 . Make sure Mianotes is running. Step 2 . Create an install script in Mianotes Settings and run it on the same machine where Claude Code runs. Step 3 . Install Claude Code CLI. Claude Code supports MCP servers, so you can add Mianotes as a stdio server. After installation completes, open a terminal in the project you want to work in and start Claude Code: Step 4 . Add the MCP server: If you installed Mianotes from package, use command mianotes mcp . If you installed it from source, use command /path/to/mianotes web service/.venv/bin/mianotes mcp . Step 5 . Verify the server is configured: Step 6 . Within Claude Code (desktop app), check server status: The Mianotes server should appear in the MCP server list. Then ask Claude Code to use Mia, for example: MCP tools The MCP surface includes these tools: | Tool | Purpose | | | | | list workspaces | List configured Mianotes workspaces. | | list folders | List folders in a workspace. | | create folder | Create a folder. | | list notes | List notes. | | read note context | Read a note by workspace, folder name, and note title. | | get note | Get a note by ID. | | create note | Create a note from text using a folder ID. | | create note in folder | Create a note from text using a workspace and folder name. | | create note from url | Create a note from a URL and queue parsing. | | update note | Update a note. | | set tags | Replace a note's tags. | | search notes | Search Markdown notes. | Most tools accept a workspace argument. Agents should pass the workspace name when the user provides one. If omitted, Mianotes uses the current or default workspace. URL ingestion results create note from url returns the same ingestion response as the REST API. It does not wait for the page to be converted before returning. The response includes: note id job id job status note api url job api url the nested note object the nested job object Agents should poll job api url until the job reaches succeeded , then call get note with note id to retrieve the finished Markdown content. File ingestion File ingestion is available through the REST API: Use the REST file upload endpoint when an agent needs to send local files to Mia. Design rule The MCP server stays thin. It does not read database files, bypass REST permissions, or write files directly. The API remains the single authority for permissions, parsing, job state, and persistence."
  },
  {
    "section": "Connecting tools",
    "folder": "Connecting tools",
    "title": "Codex, Claude, Cursor, and other agents",
    "path": "connecting-tools/codex-claude-cursor-and-other-agents-424ea9a7.html",
    "text": "Mianotes is intended to be useful for Codex, Claude Code, OpenClaw, Cursor, VS Code agents, Slack bots, and local automation. The core idea is the same for every tool: give the agent safe Mianotes API credentials and clear instructions, then ask it to write durable notes as it works. Installed agent instructions The Mianotes setup scripts install local Mianotes skill files for Codex and Claude: The web app can generate a one time install script that writes the agent environment to /.mianotes/env and installs the skill files for Codex and Claude. Environment for agents Agents need access to the API URL and user token: Generate the install URL from Mianotes Settings and run the command on the machine where the agent runs. The command can be used once and expires after 24 hours. MCP capable agents For MCP capable clients, configure the client to start: The MCP server reads MIANOTES API URL and MIANOTES API KEY . It exchanges the API key for a short lived agent session, then calls the REST API with that session token. REST capable agents Any agent that can make HTTP requests can use the REST API directly. Minimal search example: Minimal note creation example: Safety rule Treat any agent with a valid Mianotes token as trusted. Mianotes can authenticate and audit the caller, but it cannot sandbox an agent that already has filesystem access to sensitive local files."
  },
  {
    "section": "Connecting tools",
    "folder": "Connecting tools",
    "title": "Agent playbook",
    "path": "connecting-tools/agent-playbook-0dd3f7af.html",
    "text": "This page gives practical patterns for agents using Mianotes. Before starting work 1. Search Mianotes for relevant context. 2. Read the most relevant notes fully. 3. Create a new note if the task is new. 4. Add tags that will help other agents find it. Example search: After file or URL ingestion If the agent creates a note from a file or URL, the response will include a job api url . Poll the job until it succeeds. User prompts may look like: Then call the note URL to read the final Markdown."
  },
  {
    "section": "Mia",
    "folder": "Mia",
    "title": "Meet Mia",
    "path": "mia/meet-mia-eef07d25.html",
    "text": "Mia is the built in AI agent for Mianotes. It helps humans and other agents turn messy input into reusable Markdown notes. What Mia does Mia can help with: converting documents, images, links, text, and audio into Markdown notes extracting key information from source material improving note structure summarising long notes rewriting notes for clarity preparing knowledge for reuse by humans or agents Mia belongs in the backend Mia runs as a backend service and agent layer, not frontend only behaviour. The web app can collect prompts and show results, but the backend owns: permissions ownership parsing behaviour persistence LLM provider calls job state share access agent capabilities This keeps browser users, REST clients, automation scripts, and MCP agents under one capability model. Provider boundary Mia calls models through a provider boundary. Supported provider values: Local mode uses the same OpenAI client shape against an OpenAI compatible endpoint, which makes Ollama style tools fit the same code path. Mia and jobs File and URL ingestion use background jobs. Mia prompts are synchronous and do not create jobs. Use jobs for conversion work that may take time. Use Mia prompts for immediate analysis of an existing note."
  },
  {
    "section": "Mia",
    "folder": "Mia",
    "title": "Prompting Mia",
    "path": "mia/prompting-mia-cb05236e.html",
    "text": "Mia is prompted through the note prompt endpoint. Endpoint Authentication: Request Mianotes reads the current note Markdown, sends the prompt and note content to the configured LLM provider, and waits for the response. Response Important behaviour Mia prompts: are synchronous; do not create jobs; are private; do not update the note; return Markdown only. If you want Mia's answer to become part of the note, copy the returned Markdown into an update request or paste it in the web app. Example prompts Errors An empty Mia prompt returns 422 : If Mia is not configured, or the configured LLM provider is unavailable, the API returns 503 . Prompting style Good prompts are specific about the output format. Good: Weak:"
  },
  {
    "section": "Mia",
    "folder": "Mia",
    "title": "Parser and ingestion pipeline",
    "path": "mia/parser-and-ingestion-pipeline-b7ce958f.html",
    "text": "Mianotes converts files and URLs into Markdown through an adapter based parser layer. The default adapter uses Microsoft MarkItDown. Mianotes adds local source storage, web page cleanup, OCR fallback behaviour, and background job tracking around that parser boundary. Supported source types The parser can handle common source types such as: office documents PDFs images audio HTML text formats archives YouTube URLs and other URLs supported by the parser stack The file upload endpoint supports these extensions: File ingestion For normal file uploads: 1. The API receives file bytes 2. Mianotes stores the original file inside the note's sources/ directory 3. Mianotes creates a note with status pending parse 4. Mianotes creates a parse file job 5. The parser converts the source to Markdown 6. The note is updated when parsing succeeds Clients should poll the job endpoint until the job reaches succeeded URL ingestion For direct links to supported files, such as PDFs, documents, images, text files, and audio files: 1. Mianotes detects the file extension from the URL 2. The original file is downloaded into the note's sources/ directory 3. Mianotes passes the saved file to the same parser path used for uploads For web pages: 1. Mianotes downloads HTML using a browser like user agent 2. The raw HTML is stored as the source file 3. trafilatura extracts the main readable page content 4. Mianotes passes a cleaned HTML document to MarkItDown 5. If extraction fails, Mianotes falls back to the raw saved HTML file This removes most navigation menus, headers, footers, sidebars, discussion widgets, and page chrome while preserving useful links, images, and tables. Image ingestion Image files are handled differently from PDFs and office documents Mianotes first runs MarkItDown's image converter so the source is handled by the same parser adapter as other files. It then tries local Tesseract OCR for: Before using Tesseract, Mianotes checks that the binary can actually run. For screenshots and UI captures, it also runs a preprocessed OCR pass that increases contrast and image size before choosing the best OCR result. When OpenAI is configured with a multimodal model, Mianotes can use a cloud image fallback only when Tesseract cannot extract useful text. For PDFs, office files, spreadsheets, and presentations whose normal MarkItDown conversion returns no text, Mianotes retries MarkItDown with plugins and the configured LLM client. This enables OCR support from packages such as markitdown ocr without adding a new parser API surface. Some OCR plugins wrap extracted page text in [Image OCR] markers and fenced Markdown code blocks. Mianotes removes that wrapper before saving the note so the extracted headings, lists, and paragraphs render as normal Markdown. If a file still cannot be parsed, Mianotes saves a friendly message instead of an empty note: If an image cannot be read locally and no OpenAI image fallback is configured, Mianotes saves: Audio and video Install ffmpeg separately if you plan to parse audio or video sources. Parser design rule Keep specialist parsers behind the same adapter boundary so the API contract stays stable."
  },
  {
    "section": "API Reference",
    "folder": "API Reference",
    "title": "API overview",
    "path": "api-reference/api-overview-aa4fc408.html",
    "text": "The Mianotes web service exposes a local REST API. All endpoints return JSON unless the endpoint explicitly returns stored file bytes. Base URL Local development and local deployments use port 8200 by default: Authentication Protected endpoints accept one of: browser session cookie created by the auth flow; user scoped API key installed by Settings Connect tools ; scoped per user API token created through /api/tokens ; short lived agent session token returned by POST /api/auth/agent session . Bearer token shape: Agent clients may call the REST API directly with their user scoped API key, or exchange that key for a short lived agent session: Mianotes identifies the user from the bearer token. The returned session token is also sent as Authorization: Bearer <token . Common errors Not authenticated Permission denied Validation error API endpoints Health and storage GET /api/health GET /api/storage Auth POST /api/auth/check email POST /api/auth/join POST /api/auth/login GET /api/auth/session POST /api/auth/logout Tokens POST /api/tokens GET /api/tokens DELETE /api/tokens/{token id} POST /api/settings/api key POST /api/install/skill Users POST /api/users GET /api/users GET /api/users/{user id} PATCH /api/users/{user id} POST /api/users/{user id}/photo DELETE /api/users/{user id} Folders POST /api/folders GET /api/folders GET /api/folders/{folder id} PATCH /api/folders/{folder id} DELETE /api/folders/{folder id} Notes POST /api/notes/from text POST /api/notes/from file POST /api/notes/from url GET /api/notes GET /api/notes/{note id} PATCH /api/notes/{note id} PATCH /api/notes/{note id}/star DELETE /api/notes/{note id} Sharing POST /api/notes/{note id}/share DELETE /api/notes/{note id}/share GET /api/notes/shared/workspaces/{workspace id}/{token} GET /api/notes/shared/workspaces/{workspace id}/{token}/files/{source file id} GET /api/notes/shared/workspaces/{workspace id}/{token}/avatar Guest share routes include the workspace id so the service can resolve the shared note directly from the correct workspace database. Tags GET /api/tags PUT /api/notes/{note id}/tags Mia POST /api/notes/{note id}/prompt Search and context GET /api/search GET /api/context Jobs GET /api/jobs GET /api/jobs/{job id} Publishing GET /api/publish/themes GET /api/publish/draft POST /api/publish GET /api/publish/{site id}/download Settings GET /api/settings/storage POST /api/settings/storage/locations PATCH /api/settings/storage/active POST /api/settings/api key Stored files GET /markdown/{file path} GET /api/workspaces/{workspace id}/notes/{note id}/markdown GET /api/workspaces/{workspace id}/notes/{note id}/source files/{source file id} GET /api/workspaces/{workspace id}/notes/{note id}/images/{file path} Health check Authentication: none. Example response: Storage capacity Authentication: browser session or API token with notes\\:read . Returns cached capacity information for the drive that stores Mianotes data. The service refreshes this snapshot at most once per hour."
  },
  {
    "section": "API Reference",
    "folder": "API Reference",
    "title": "Authentication and users",
    "path": "api-reference/authentication-and-users-be15f56d.html",
    "text": "This page documents authentication, session, token, and user endpoints. Check email Authentication: none. Request: First user setup response: Existing user response: Join Authentication: none. If no user exists yet, this endpoint creates the first admin user and stores the password verifier. Request: The response sets an HTTP only mianotes session cookie. Login Authentication: none. Request: The response sets an HTTP only mianotes session cookie. Get current session Authentication: session cookie or bearer token. Example: Create agent session Authentication: bearer API key or scoped API token. Required header: Mianotes validates the bearer token, identifies the user who owns it, signs that user into a short lived session token, and returns the token for follow up requests. The raw API key is not embedded in the session token. Response: Logout Authentication: none. Response: 204 No Content . Create scoped API token Authentication: session cookie or bearer token with tokens\\:write or admin . Request: The raw token is returned only once. List API tokens Authentication: session cookie or bearer token with tokens\\:read or admin . Query parameters: | Parameter | Description | | | | | user id | Optional user whose tokens should be listed. Admin only for other users. | | include revoked | Include revoked tokens. Defaults to false . | Raw token values are not returned by this endpoint. Revoke API token Authentication: session cookie or bearer token with tokens\\:write or admin . Response: 204 No Content . Create service level API key Authentication: admin session or bearer token with admin . Creates or rotates the service level key used by trusted service integrations. Most users should use Settings Connect tools instead, because that flow creates a user scoped key and installs the Mianotes agent instructions on the local machine. Response: The raw key is returned only once. User endpoints | Method | Path | Purpose | Auth | | | | | | | POST | /api/users | Create a user directly | Admin | | GET | /api/users | List users | users\\:read or admin | | GET | /api/users/{user id} | Get one user | users\\:read or admin | | PATCH | /api/users/{user id} | Update profile | profile owner or admin | | POST | /api/users/{user id}/photo | Upload profile photo | profile owner or admin | | PATCH | /api/users/{user id}/admin | Change admin access | Admin | | PATCH | /api/users/{user id}/password | Update password | Admin | | DELETE | /api/users/{user id} | Delete user | Admin | Profile photos accept JPG or PNG files, crop and resize to 200x200 , convert to JPEG, and store the resized image under data/.profiles ."
  },
  {
    "section": "API Reference",
    "folder": "API Reference",
    "title": "Folders, notes, and tags API",
    "path": "api-reference/folders-notes-and-tags-api-8e8e2b7a.html",
    "text": "This page documents the core knowledge endpoints. Folder object Create folder Authentication: session cookie or bearer token with folders\\:write or admin . Request: The folder owner is always the current user. List folders Authentication: session cookie or bearer token with folders\\:read or admin . Query parameters: | Parameter | Description | | | | | user id | Return folders created by a specific user. | | include archived | Include archived folders. Defaults to false . | Update folder Authentication: session cookie or bearer token with folders\\:write or admin . Admins can update any folder. Normal users can update only folders they created. Archive folder Authentication: session cookie or bearer token with folders\\:write or admin . Archived folders are hidden unless include archived=true . Restore folder Authentication: session cookie or bearer token with folders\\:write or admin . Admins can restore any archived folder. Normal users can restore only folders they created. Request: Both fields are optional. If the original folder slug is already taken, Mianotes creates a unique restored slug. Create note from text Authentication: session cookie or bearer token with notes\\:write or admin . Request: Tags are limited to 5 per note. Create note from file Authentication: session cookie or bearer token with notes\\:write or admin . Request: multipart/form data . | Field | Required | Description | | | :| | | folder id | Yes | Folder ID for the note. | | file | Yes | Source file to upload. | | title | Yes | Note title. | The response returns a pending note plus a queued parse file job. Clients should poll job api url , then fetch note api url after success. Supported upload extensions: Create note from URL Authentication: session cookie or bearer token with notes\\:write or admin . Request: The response returns a pending note plus a queued parse url job. List notes Authentication: session cookie or bearer token with notes\\:read or admin . Query parameters: | Parameter | Description | | | | | user id | Return notes created by a specific user. | | folder id | Return notes in a specific folder. | | starred | Return notes starred or not starred by the authenticated user. | List responses include metadata and a stored summary, but not full Markdown text. Get note Authentication: session cookie or bearer token with notes\\:read or admin . Returns a full note including Markdown text , owner, folder, source files, tags, sharing metadata, and action hints. Update note Authentication: session cookie or bearer token with notes\\:write or admin . Request fields: Admins can update any note. Normal users can update only notes they created. Star note Stars are private per user. Request: Delete note Deletes the note, stored Markdown file, source files, tags, share records, and job records. Files outside the workspace are not deleted. Tags List global tags: Replace a note's tag set: Request:"
  },
  {
    "section": "API Reference",
    "folder": "API Reference",
    "title": "Comments, Mia prompts, and jobs API",
    "path": "api-reference/comments-mia-prompts-and-jobs-api-a05c187a.html",
    "text": "This page covers private Mia prompts and background jobs. Send a private Mia prompt Use the note prompt endpoint: Authentication: session cookie or bearer token with notes\\:write or admin . Response: a prompt response with 200 OK . Mia prompt responses do not create jobs and do not update the note. Job object List jobs Authentication: session cookie or bearer token with notes\\:read or admin . Admins can see all jobs. Normal users see only their own jobs. Query parameters: | Parameter | Description | | | | | note id | Return jobs related to one note. | | status | Return jobs with a specific status. | | limit | Maximum number of jobs to return. Defaults to 50 ; maximum 200 . | | cursor | Cursor from the previous response for the next page. | | include logs | Include decoded console log entries in the list response. Defaults to false . | | include payloads | Include decoded input and result payloads in the list response. Defaults to false . | The list endpoint returns a bounded page: By default, list responses do not decode large job logs, input payloads, or result payloads. Use the single job endpoint when you need the complete console output. Get job Authentication: session cookie or bearer token with notes\\:read or admin . This endpoint returns the full job record, including decoded input, result, and console log entries. Job statuses Agents should poll the job endpoint when file or URL parsing is pending."
  },
  {
    "section": "API Reference",
    "folder": "API Reference",
    "title": "Search, context, sharing, and stored files API",
    "path": "api-reference/search-context-sharing-and-stored-files-api-ebb8370a.html",
    "text": "This page covers endpoints that retrieve existing knowledge. Search notes Authentication: session cookie or bearer token with notes\\:read or admin . Query parameters: | Parameter | Required | Description | | | :| | | q | Yes | Search query. Minimum length is 1. | | limit | No | Maximum matches. Defaults to 50 ; maximum 100 . | Example: Response shape: Mianotes searches saved Markdown files with ripgrep and joins file matches back to note metadata. Get context Authentication: session cookie or bearer token with notes\\:read or admin . This endpoint retrieves Markdown context from notes in a specific folder. It is designed for agents following prompts such as: Query parameters: | Parameter | Required | Description | | | :| | | folder | Yes | Folder name or slug. | | title | Yes | Note title or context phrase. | | limit | No | Maximum notes. Defaults to 5 ; maximum 20 . | The service first looks for matching note titles in the requested folder. If no title match is found, it searches Markdown files in that folder and returns matching notes. Share note Create or replace a read only share URL: Authentication: session cookie or bearer token with share\\:write or admin . Response: The returned URL includes the workspace id. This lets Mianotes resolve the shared note directly from the correct workspace database instead of searching across every configured workspace. Disable sharing: Get shared note Authentication: none. Returns a shared note without requiring a session or bearer token. workspace id is the configured workspace id, not the workspace name shown in the UI. Invalid workspace ids or invalid share tokens return 404 . Legacy token only share URLs are not workspace addressed and should not be generated by new clients. Get shared source file Authentication: none. Returns a source file attached to a shared note. Get shared author avatar Authentication: none. Returns the shared note author's avatar when the shared note can be resolved and the author has an avatar. Get published static HTML Authentication: none. Returns generated static HTML files from published sites. The /html root redirects to the latest published version. Get published Markdown Unauthenticated callers can only read Markdown or source files for notes marked as published. Get note owned stored files Stored file access is database addressed. Mianotes resolves the workspace, note, and source file record before serving bytes. Authentication: session cookie or bearer token with notes\\:read or admin . These routes only return files that belong to the requested note in the requested workspace. Database files and arbitrary workspace files are never served."
  },
  {
    "section": "API Reference",
    "folder": "API Reference",
    "title": "Publishing and settings API",
    "path": "api-reference/publishing-and-settings-api-96cf506f.html",
    "text": "This page covers static site publishing and admin settings endpoints. List publish themes Authentication: session cookie or bearer token with notes\\:read or admin . Response: Get publish draft Authentication: session cookie or bearer token with notes\\:read or admin . Query parameters: | Parameter | Description | | | | | theme | Theme ID. Defaults to mialight . | | folder id | Optional folder scope. Omit to publish all folders. | | tag id | Optional tag scope. | navigation always contains every note that will appear in the static site. updated notes only lists notes whose generated paths were not present in the previous publish navigation for the same scope. Publish site Authentication: session cookie or bearer token with notes\\:write or admin . Request: Response includes site url and download url . Download published site Authentication: session cookie or bearer token with notes\\:read or admin . Returns a ZIP archive of the generated static site. Mianotes streams the archive from a temporary file and enforces configured byte and file count limits. If the generated site is too large to download as one ZIP, the endpoint returns 413 . Get storage settings Authentication: admin session or bearer token with admin . Returns the active workspace and available workspaces with lightweight database stats. The API still uses storage in these endpoint names, but the product language in the web app is workspace . Add workspace Authentication: admin session or bearer token with admin . Request: Mianotes creates data/workspaces/<workspace id .db if needed and initialises the workspace schema. The selected folder stores workspace content such as Markdown notes, source files, and published output. Switch active workspace Authentication: admin session or bearer token with admin . Request: The response includes session ended: false . Workspace switching is stored on the current session, so the user stays signed in. If Mia has queued or running jobs, the API returns 409 and asks the user to try again after those jobs finish. Create service level API key Authentication: admin session or bearer token with admin . Creates or rotates the service level key used by trusted service integrations. Most users should use Settings Connect tools instead, because that flow creates a user scoped key and installs the Mianotes agent instructions on the local machine. The raw key is returned only once:"
  },
  {
    "section": "System",
    "folder": "System",
    "title": "Architecture",
    "path": "system/architecture-df6e4654.html",
    "text": "Mianotes Web Service is a FastAPI application with filesystem first note storage and a small relational index. Core principles The filesystem stores user facing note content. Markdown is the durable note format. SQLite stores indexes, metadata, relationships, sessions, tokens, and jobs. API responses are JSON unless an endpoint explicitly returns a stored file. The backend owns permissions, parsing, job state, and persistence decisions. Browser users and AI agents use the same backend capability model. High level components Storage layout Generated notes and source files live inside each workspace folder: Folder rows store their filesystem path. Note rows store their Markdown filename. The full path is derived from those two values. Later title edits do not rename files. This keeps URLs and filesystem references stable. Database responsibilities SQLite tracks: users folders notes metadata source files tags sessions API tokens share tokens jobs The repository and service boundaries keep storage concerns separate from the public API contract. Jobs Long running work is represented with durable rows in the mia jobs table. Status transitions: This gives the web app and agent clients a stable polling model for file and URL ingestion. Search Saved Markdown files are searched with ripgrep . The search service joins file matches back to note metadata before returning JSON results. This keeps Markdown files as the source of note text while still giving API clients structured note IDs, titles, users, folders, tags, and timestamps. Authentication The web app uses long lived HTTP only cookie sessions. Agents and automation scripts use bearer tokens: Token hashes are stored in the database. Raw token values are returned only once when created."
  },
  {
    "section": "System",
    "folder": "System",
    "title": "Storage folders and database",
    "path": "system/storage-folders-and-database-a6fc7f27.html",
    "text": "Mianotes uses SQLite by default. In the UI, admins choose local workspaces. Workspace folders store Markdown notes, source files, published HTML, and other content files. Private SQLite databases live under the Mianotes data directory. Default databases On first start, Mianotes reads settings.json if it exists, then creates a runtime workspaces.json file in the web service root if it does not already exist. A fresh install uses: data/system.db stores global users, sessions, API key verification, and global settings. data/workspaces/default.db is the default workspace database. Additional workspaces use the same pattern: This keeps database files in the application data area and leaves selected workspace folders cleaner. workspaces.json workspaces.json lists the local workspaces available to the app. Example: Each location points to a workspace folder. Each workspace has its own notes, folders, tags, source records, jobs, shares, and publishing history in data/workspaces/<workspace id .db . Users, sessions, API keys, and global settings live in data/system.db . settings.json settings.json contains project level service settings. It can set the server host and port, model providers, binary lookup paths, and the database adapter. Secrets should not be written directly into settings.json . Use environment placeholders such as: Mianotes resolves env.MIANOTES LLM API KEY from the process environment or .env . The database section currently supports SQLite: If MIANOTES DATABASE URL is not set, Mianotes uses data/system.db for global app state. Other adapters are intentionally rejected for now so workspace data cannot be mixed into one server database before the schema supports it. Database initialisation Mianotes creates and updates database tables in two places: 1. on service startup; 2. when mianotes web service init db is run. Startup initialises data/system.db , then initialises every configured workspace database from workspaces.json . SQLite file databases are opened in WAL mode. This creates sidecar files such as system.db wal and system.db shm , which are normal SQLite runtime files and should not be committed. Switching workspaces All signed in users can switch workspace from the workspace switcher next to the breadcrumb. Admins can add workspaces from Settings. When a user switches workspace, Mianotes: 1. stores the selected workspace on that user's session; 2. routes workspace content API calls to data/workspaces/<workspace id .db ; 3. keeps the browser session signed in; 4. leaves other users on their own selected workspace. Switching workspace is per user session. It is not global process state. Creating a new workspace Admins can add a new local workspace from Settings. Mianotes creates data/workspaces/<workspace id .db and initialises the workspace schema. Users remain global and can use the new workspace without signing up again. Private files Do not commit these to a public repository: Database files are never served by the file API. Source and Markdown files are served through controlled routes, but system.db , workspace databases, and SQLite sidecars are blocked."
  },
  {
    "section": "System",
    "folder": "System",
    "title": "Security",
    "path": "system/security-f4e332c7.html",
    "text": "Mianotes is local first, but local first does not mean risk free. Treat any user, agent, or script with a valid Mianotes token as trusted. Access methods Protected APIs can be accessed by: browser users with a cookie based session tools using a user scoped MIANOTES API KEY installed by Settings Connect tools narrower automations using scoped per user API tokens created through /api/tokens Bearer tokens are sent as: API tokens are private. Mianotes stores only a derived public hash in data/system.db , so the same running service can switch workspaces without storing raw tokens in any database. What authentication does and does not do Authentication verifies the caller. It does not sandbox the caller. If an agent can read a sensitive file on the local machine, Mianotes cannot prevent that agent from uploading the file contents as text, a renamed file, or a generated note. Mianotes can audit who sent data. It cannot prove where uploaded bytes originally came from. Best practices for agents Do not give untrusted agents filesystem access to sensitive files. Run agents with the least filesystem access they need. Give agents project specific working directories instead of access to a whole home directory. Use Settings Connect tools to install user scoped credentials for trusted local agents. Use scoped tokens when an automation needs narrower access. Revoke agent tokens when agents no longer need access. Avoid giving hosted or third party agents direct access to local folders unless you trust the agent and its operator. Review activity logs after allowing a new agent or automation to use Mianotes. File uploads and parsing Uploaded files are first saved into the Mianotes data directory, then parsed from that controlled location. Mianotes APIs and MCP tools accept uploaded file bytes, URLs, text, or existing Mianotes source file IDs. They do not provide a general purpose endpoint for reading arbitrary local filesystem paths from the server. This prevents clients from asking the Mianotes service to parse arbitrary host paths. URL indexing URL ingestion fetches data from the network. Admins can configure blocked domains to prevent Mianotes from fetching URLs from sensitive or disallowed hosts. Recommended blocked targets include: internal metadata services private administration panels internal only hostnames domains that should never be indexed into shared knowledge Hosted deployments should also consider blocking private network ranges unless the workspace is intentionally allowed to index internal resources. Activity auditing (coming soon) The Activity screen lets admins audit: who uploaded a file when the upload happened the original filename the source type which note or folder the upload belongs to which user or agent token performed the action Audit logs help with accountability and investigation, but they do not prevent a trusted caller from sending data it can already access. Deployment guidance For local only use, bind the service to: Only bind Mianotes to 0.0.0.0 or another network interface when you understand the security implications. Recommended deployment practices: use a dedicated operating system user for the Mianotes service store Mianotes data in a directory owned by that service user do not run Mianotes as root use a firewall when exposing Mianotes on a network use HTTPS when hosting beyond a trusted local network keep API tokens private and rotate them if exposed What Mianotes can protect Mianotes can: require authentication for protected APIs require bearer API tokens for agents store source metadata for auditability restrict parsing to Mianotes controlled files block configured file paths and domains let admins review activity Mianotes cannot: sandbox an agent that already has access to sensitive local files prove that uploaded content did not come from a secret file stop a trusted local process from copying data it can already read replace operating system permissions, firewall rules, or agent sandboxing"
  },
  {
    "section": "Contribute",
    "folder": "Contribute",
    "title": "Development",
    "path": "contribute/development-a221d01b.html",
    "text": "This page is for people working on the Python web service locally. Install system dependencies Mianotes currently supports Python 3.11, 3.12, 3.13, and 3.14 for source installs. macOS: Linux: Install tesseract too if you are working on image OCR. Start the development server The script: creates .venv if needed; installs Mianotes with development dependencies; installs the Codex and Claude skills; initializes the database; starts the API on port 8200 with reload enabled. Use ./start dev.sh only when developing the backend. For normal local use, run ./start.sh . Use a different host or port Manual setup The installer copies the repository's Mianotes skill to: Manual run For auto reload: The default API URL is: FastAPI exposes interactive local API docs at: Service run For always on local boxes, use a systemd service. Manual run should remain the default for docs and local development. The service command should be: After installing a service file: Development configuration To run multimodal models locally on a 16GB Mac mini, we recommend Qwen3 VL, Qwen2.5 VL, or Llama 3.2 Vision (11B). Get the Ollama installer directly from their site, open your terminal, and run: ollama run qwen3 vl . For OpenAI and other cloud LLMs:"
  },
  {
    "section": "Contribute",
    "folder": "Contribute",
    "title": "Maintainer checklist",
    "path": "contribute/maintainer-checklist-dc4a28a9.html",
    "text": "Use this checklist before publishing a new Mianotes release or updating the documentation. Documentation structure Root TOC.md exists. Quick start information is near the top of the docs. Agent docs are separated from human workflow docs. API reference is grouped by task, not dumped as one long page. Security warnings are explicit and easy to find. Configuration examples use MIANOTES variables consistently. Local first positioning is clear but not overstated. Release checks Run: Manual smoke checks Fresh install starts with ./start.sh . First user setup works. Text note creation works. File upload creates a job and eventually updates the note. URL ingestion creates a job and eventually updates the note. Search returns note metadata and excerpts. Mia prompt returns a prompt response without changing the note. API key creation returns the raw key once. Scoped token permissions produce expected 401 and 403 behaviour. MCP server starts with MIANOTES API URL and MIANOTES API KEY . Share link can be created, loaded without auth, and revoked. Publish draft navigation includes all publishable notes for the selected scope. Publish draft New notes only lists paths that were not in the previous publish. Static sites render code blocks, tables, admonitions, search, and On this page . mialight and miadark both render readable article pages. Published site preview and ZIP download both work. Security checks .env is not committed. workspaces.json is not committed. data/ , system.db , workspace database files, and SQLite sidecar files are not committed. Example tokens are fake. Docs remind users not to give untrusted agents filesystem access to sensitive files. File API blocks database files. Hosted deployment docs mention firewall and HTTPS. Documentation update checklist When an endpoint changes: 1. Update the relevant API reference page. 2. Update agent docs if the endpoint is useful to agents. 3. Update security docs if auth or scope behaviour changed. 4. Add or update examples. 5. Run a link check across Markdown files. When the UI changes: 1. Update human workflow docs. 2. Update first run docs if setup changed. 3. Update theming/customisation docs if visual configuration changed. 4. Keep screenshots optional so docs do not break when images are missing."
  },
  {
    "section": "Contribute",
    "folder": "Contribute",
    "title": "Testing",
    "path": "contribute/testing-620bac7d.html",
    "text": "Run checks from the repository root. Unit and API tests The test suite covers the current API behaviour, auth flow, notes, folders, tags, search, MCP helpers, parser adapter behaviour, and synchronous Mia prompts. Automated tests use temporary data folders and do not write to the local data/ directory. For static publishing changes, run the focused publish suite: It checks theme listing, draft generation, navigation/new note diffing, HTML and Markdown output, public file serving, ZIP downloads, and replacement of an existing version. SQLite query plan checks The normal test suite includes lightweight SQLite query plan checks: These tests use EXPLAIN QUERY PLAN to make sure common note, publishing, and job queries keep using the intended indexes. They are fast and run as part of the normal test suite. For larger local checks, run the opt in performance test: This creates a temporary SQLite workspace with 10,000 notes, related folders, tags, jobs, and published site rows. It runs ANALYZE , prints the query plans and rough timings, and verifies that hot queries use the expected composite indexes without temporary sort tables. Performance tests are skipped by default. Use them before changing indexes, query builders, pagination, publishing lookups, or job listing behaviour. Manual tests with temporary storage Use the temporary storage wrapper when you want to run manual smoke tests, seed data, or start a throwaway service: The wrapper creates a temporary Mianotes data folder, points the service at a temporary system.db , runs the command, and removes the temporary folder when the command exits. To start a disposable local service: Anything created during that run is removed when the service stops. Linting Ruff enforces the configured Python lint rules in pyfolder.toml . Compile check Use this to catch syntax errors across the source and test tree. Full local check Test design notes Tests should not require real OpenAI credentials or a local LLM server. Code that calls external model providers should be isolated behind service boundaries and tested with fakes or mocks. Parser tests should avoid requiring live network access. URL behaviour should be tested by faking the HTTP fetch boundary and verifying that local files are passed to MarkItDown. Useful manual smoke tests 1. Start a temporary service. 2. Create the first user. 3. Create a folder. 4. Create a text note. 5. Search for the note. 6. Upload a small supported file. 7. Poll the parsing job. 8. Prompt Mia with a fake or configured provider. 9. Create and revoke a share link. 10. Create and revoke an API token. 11. Publish a small static site and open the preview. 12. Download the static site ZIP."
  },
  {
    "section": "Contribute",
    "folder": "Contribute",
    "title": "Theming and customisation",
    "path": "contribute/theming-and-customisation-3120957f.html",
    "text": "Mianotes has two kinds of customisation: runtime configuration for the web service, parser, storage, and model providers; static publishing themes used when notes are exported as documentation sites. Treat the main web app visual design as application code. Static site publishing themes use a small theme boundary. Static publishing themes Mianotes ships two static publishing themes: | Theme ID | Name | Purpose | | | | | | mialight | Mialight | Default light documentation theme. | | miadark | Miadark | Dark documentation theme. | | miadev | Miadev | A theme for developers. | | miadocs | Miadocs | A calm light blue theme. | Theme assets live in: Each theme directory contains: The theme.json file defines the theme metadata returned by GET /api/publish/themes, styles.css controls the published site appearance. site.js handles static site behaviour such as navigation, search wiring, article state, and previous version links. Static themes should render these Markdown features well: headings and article title hierarchy paragraphs, ordered lists, and unordered lists inline code and fenced code blocks Markdown tables links GitHub style admonitions such as [!TIP] , [!NOTE] , and [!WARNING] the generated On this page column previous version links when showPreviousVersions is enabled When adding or changing a theme, run the publishing tests and manually publish a small site that includes code blocks, tables, admonitions, and enough headings to populate the right hand article navigation. Runtime customisation Supported configuration areas: service host and port allowed workspaces LLM provider and model image OCR fallback model user scoped API keys for connected tools scoped automation tokens parser adapters Ports Workspace customisation Default: Admins can switch between allowed local folders from the Settings screen. LLM customisation Mia supports: Example local setup: Web app theme conventions Keep these decisions documented when changing the web app visual system: colour tokens typography scale spacing scale dark mode behaviour logo and app icon placement Markdown rendering styles note status badges accessibility contrast rules whether themes are per user, per workspace, or build time only"
  },
  {
    "section": "Contribute",
    "folder": "Contribute",
    "title": "Troubleshooting",
    "path": "contribute/troubleshooting-60ffcaf9.html",
    "text": "Issue: macOS audio uploads fail with Bad CPU type Mianotes uses MarkItDown to convert uploaded files into Markdown. For audio files, MarkItDown calls local audio tools such as ffprobe , ffmpeg , flac , and metaflac . On Apple Silicon Macs, audio uploads can fail if old Intel binaries in /usr/local/bin are found before Apple Silicon Homebrew binaries in /opt/homebrew/bin . The Jobs console may show errors like: or: Follow these steps in order. Step 1: Check which binaries are being used Run this from the same shell that starts Mianotes: A broken Apple Silicon setup usually points to /usr/local/bin and reports x86 64 binaries: A healthy setup should point to /opt/homebrew/bin and report arm64 binaries: Step 2: Reinstall the Apple Silicon audio tools Install or reinstall the required packages with Homebrew: Then temporarily put Apple Silicon Homebrew first in your current shell: Check the binaries again: If the output now points to /opt/homebrew/bin and reports arm64 , restart Mianotes from the same shell. Step 3: Fix your shell startup file If the problem comes back in a new terminal, your shell startup file is probably putting /usr/local/bin before /opt/homebrew/bin . Inspect your startup files: Look for a line like this: That line can shadow Apple Silicon tools if it runs after the /opt/homebrew/bin line. For zsh, use this order: The important part is that the /opt/homebrew/bin line runs after any line that prepends /usr/local/bin . Reload the shell: Then check again: Step 4: Check bash as well if Mianotes starts through bash Some terminals, editor tasks, or scripts may start Mianotes through bash instead of zsh. If the service is started from bash, add the Homebrew path to /.bash profile or /.bashrc as well: Then reload bash: Step 5: Start Mianotes again After fixing PATH , restart Mianotes from a shell where the checks show /opt/homebrew/bin and arm64 . For development: For normal local use: For a temporary one command test, you can start Mianotes with the corrected PATH inline: If that fixes audio uploads, make the PATH change permanent in the shell startup file used to launch Mianotes."
  }
];
